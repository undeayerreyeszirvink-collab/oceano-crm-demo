**SKILL.md 标准定义模板**。

您可以将此作为公司内部 AI 能力资产沉淀的唯一执行标准。

# ---

**SKILL 标准定义模板 (SKILL.md)**

## **1\. 基本信息 (Basic Information)**

* **Skill 名称**: \[例如：发票字段提取 / 预算合规检查 / 采购 RFQ 协同流程\]

* **Skill ID**: \[全局唯一标识符，建议格式：tenant-business-action-skill\]

* **所属层级**: \[L1 基础原子层 / L2 行业能力层 / L3 企业专属层\]

* **Provider**: \[例如：keep / 其他指定提供商\]  
* **版本号**: V1.0

## **2\. 判定依据 (Boundary Determination)**

*注：此处需明确回答为什么该 Skill 属于当前层级，以防止边界漂移。*

* **复用范围**: \[跨行业通用 / 同行业跨企业复用 / 仅限本企业/租户\]

* **语义依赖**: \[无 / 依赖行业通用语义 / 强依赖企业私有语义\]

* **判定口诀验证**: \[例如：换个行业还能用 (L1) / 换个企业还能用 (L2) / 只有这家企业这样干 (L3)\]

## **3\. Ontology 依赖 (Ontology Dependencies)**

*原则：Ontology 先行。Skill 是在 Ontology 定义的世界中执行任务。*

* **实体 (Entities)**: \[例如：采购单、供应商、窑炉参数、预算条目\]

* **角色 (Roles)**: \[例如：财务审核人、工艺工程师、采购经理\]

* **状态 (Statuses)**: \[例如：待需求确认、待报价、已审批\]

* **规则约束 (Rules)**: \[例如：未完成预算校验不得下发采购单\]

## **4\. 执行逻辑与 SOP 拆解 (Execution Logic)**

*按照 SOP 拆解三类语句：识别/抽取 (L1)、判断/评估 (L2)、执行/流程 (L3)。*

| 步骤 | 动作类型 | 逻辑描述 | 依赖项 |
| :---- | :---- | :---- | :---- |
| 1 | \[识别/判断/执行\] | \[简述该步骤的核心处理逻辑\] | \[关联的 L1/L2 Skill\] |
| 2 | \[识别/判断/执行\] | \[例如：调用 L2 进行预算合规性检查\] | \[budget-check\] |
| 3 | \[识别/判断/执行\] | \[例如：路由至财务节点进行审批\] | \[CLI: OA-Approval\] |

## **5\. 调用机制设计 (Invocation Architecture)**

* **类型 B 能力执行**: \[Skill 作为编排引擎的 Tool/Function 调用\]

* **类型 C 系统操作 (CLI)**: \[涉及外部状态改变的动作，必须通过 CLI Gateway\]

  * **接口**: \[例如：ERP.write / OA.submit\]

  * **审计要求**: \[所有写操作必须可追溯\]

* **HITL (人工环路)**:

  * **中断/触发点**: \[例如：信息缺失追问 / 高风险动作审批 / 异常回退确认\]

## **6\. 风险控制与反模式预防 (Risk Control)**

* **禁止事项**:  
  * \[ \] 严禁 L1 直接操作业务系统（必须经过编排和审批）。

  * \[ \] 严禁将企业私有规则硬编码进 L1。

  * \[ \] 高风险动作严禁无 HITL 自动闭环。

## **7\. 验收标准 (Acceptance Criteria)**

* **准确率**: \[例如：字段提取准确率 \>= 95%\]

* **覆盖率**: \[例如：RFQ 自动路由覆盖率 \>= 85%\]

* **时效性**: \[例如：生产异常响应延迟 \< 5秒\]

### ---

**实施建议 (Implementation Advice)：**

1. **资产沉淀**: 每一个发布的 Skill 都应作为公司的数字资产，存入 Skill 库，避免重复造轮子。

2. **持续演进**: 随着企业 SOP 的优化，只需迭代 L3 层 Skill，无需改动底层的 L1/L2，确保系统稳定性。

3. **标准化流程**: 严格遵守“文档预处理 $\\rightarrow$ Ontology 提取 $\\rightarrow$ SOP 拆解 $\\rightarrow$ 判层 $\\rightarrow$ 补全依赖 $\\rightarrow$ 审核发布”的九步法。  
